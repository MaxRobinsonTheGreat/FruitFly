const Sprite = require('./rendering/sprite');
const ImageContainer = require('./rendering/image_container').getImageContainer();


var renderer = module.exports = {
  getImageContainer: function() {
  }

  
}
